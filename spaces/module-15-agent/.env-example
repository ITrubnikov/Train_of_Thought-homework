# Скопируйте этот файл в .env (cp .env-example .env) и впишите значения.
# .env закрыт .gitignore-ом — под-токен не должен попадать в git.
# Настоящие переменные окружения важнее .env: оно заполняет только пропуски.

# Под-токен вашего жителя: интерфейс игры -> Ратуша -> вкладка «аккаунт» -> «копировать».
# С заполненным .env поле токена в интерфейсе уже предзаполнено.
COGNOPOLIS_TOKEN=

# Необязательные переопределения (дефолты подходят почти всем):
# LM_MODEL_ID=qwen2.5-7b-instruct-1m     # конкретная модель вместо автодетекта
# LM_BASE=http://localhost:1234/v1       # адрес OpenAI-совместимого сервера модели
# GATE_TOOLS=gather                      # какие инструменты идут через ворота (пусто = ворот нет)
#   Эксперимент лекции: GATE_TOOLS= выключает permission gate — агент рубит без спроса.
# AGENT_MEMORY=1                         # одна нить (thread_id) на весь чат до кнопки «Сбросить»
#   (дефолт: каждый прогон — новая нить; накопленная история топит слабую 7B)
# WEAK_MODEL_ROUTE=0                     # убрать маршрутную подсказку слабой модели
# COGNOPOLIS_BASE_URL=http://localhost:8000       # переключить на ЛОКАЛЬНЫЙ движок игры
#   (токен тогда тоже нужен локальный — из Ратуши локальной игры, не с прода)
