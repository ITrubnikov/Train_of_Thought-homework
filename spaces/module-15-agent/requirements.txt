# Пины LangGraph — ровно те же, что в ноутбуке модуля 15: свободные версии
# ломают шаблон молча (create_react_agent деприкейтнут прямо между минорами).
langgraph==1.2.9
# Мозг: клиент ChatOpenAI к OpenAI-совместимому серверу LM Studio, без ключей.
langchain-openai==1.3.5
# ascii-отрисовка графа для панели «Рельсы агента» (схема выводится из кода).
grandalf==0.8
gradio>=5,<6
requests>=2
