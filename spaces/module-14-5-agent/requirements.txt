# smolagents[mcp] тянет mcpadapt и mcp — это клиентская сторона (ToolCollection.from_mcp)
smolagents[mcp]==1.26.0
# fastmcp — серверная сторона: на нём написан cognopolis_mcp_server.py (нужен Python 3.10+)
fastmcp>=3
gradio>=5,<6
openai>=1.0
requests>=2
